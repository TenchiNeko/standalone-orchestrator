[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "standalone-orchestrator-v2"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["orchestrator_v2"]

[project.scripts]
orchestrator-v2 = "orchestrator_v2.cli:main"
